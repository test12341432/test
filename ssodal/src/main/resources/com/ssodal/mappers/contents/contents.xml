<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="contents">
	
	<select id="contents_cont" parameterType="int" resultType="contsbean">
	select * from contents where content_code=#{i}
	</select>
	
	<select id="seat_row" parameterType="int" resultType="seatbean">
	select * from seat_info where content_seat_class=#{seat_code}
	</select>
</mapper>